angular.module('dopApp')
  .config(function($stateProvider){
  })
  .controller('CoinsModalCtrl', function($scope, $auth, $http, $mdDialog, SweetAlert, CouponFactory, $userService, $paymentService) {
  }
